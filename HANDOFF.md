---
date: 2026-08-13
status: current
tags:
  - interview-workbench
  - migration
  - handoff
---

# 面试工作台迁移与 AI 交接说明

## 1. 项目位置

- 独立面试工作台：当前 `interview-workbench/` 目录
- JobHunter 总项目：当前目录的上一级仓库
- 当前开发地址：`http://127.0.0.1:4173/`
- 面试工作台与 JobHunter 主应用运行时代码隔离。迁移面试工作台时，应复制整个 `interview-workbench/`，并同时携带总项目的 `AGENTS.md`、`docs/DECISIONS.md` 和 `outputs/MVP_PRD_V0.1.md` 作为上位约束。

## 2. 给下一个 AI 的开工指令

> 先阅读 `AGENTS.md`、`interview-workbench/HANDOFF.md`、`interview-workbench/README.md`、`outputs/MVP_PRD_V0.1.md`、`docs/DECISIONS.md`、`docs/WORKFLOW.md`、`interview-workbench/docs/INTERVIEW_EVIDENCE_REVIEW_V1.md`，再按时间阅读 `interview-workbench/docs/archives/`。`.codegraph/` 存在时先用 CodeGraph 理解代码。不要回滚现有未提交改动，不要读取、打印或提交 `.env.local`、API Key、浏览器 Cookie、`node_modules/`、`dist/` 或用户简历数据。开始修改前先复现当前问题；完成独立模块后补 Obsidian 兼容归档，并运行 `npm run check`、`npm test`、`npm run build`。

## 3. 产品定位与当前闭环

独立面试工作台验证以下闭环：

1. 用户填写公司、岗位和岗位 JD；JD 按“公司 + 岗位”绑定。
2. 用户粘贴或上传简历；支持 `.txt`、`.md`、`.docx` 和文字型 `.pdf`，在浏览器内转成可编辑正文。
3. 用户手动导入面经，或主动点击 Agent Reach / OpenCLI 小红书采集。
4. 面经来源按“公司 + 岗位”隔离，并形成浏览器本地“我的面经库”。
5. 本地规则生成简历题和专业 / JD 题；DeepSeek 可在用户主动触发时基于同一批材料深度组题。
6. 用户选择单题文字或浏览器原生语音作答。
7. DeepSeek 按 `interview.evidence-review.v1` 进行无分数、证据化审阅。
8. 首轮只生成一条追问；用户可结束或回答第二轮，之后封闭并保存训练记录。

## 4. 已确认的产品决策

### 4.1 题库分类

- 主类别只有两类：`resume_deep_dive`（简历题）和 `role_capability`（专业 / JD 题）。
- “面经真题”是来源维度，不是第三种能力类别。页面筛选为：全部、简历题、专业 / JD 题、面经真题。
- 真题必须为 `provenance=source_question`，保留原文与来源；AI 衍生题必须为 `coach_generated`，不得冒充真题。
- 简历题最多选择 3 条高价值主张，每题只追一个切口：指标口径、个人贡献、决策依据、约束、取舍／失败或反事实。
- 不把职位／日期标题、联系方式和过程数量误当成果主张；完整原文放在 `sourceRefs`，题面使用短锚点。

### 4.2 DeepSeek

- DeepSeek 只在用户主动点击时使用，不做后台调用，不失败后转发其他模型。
- 保留“本地规则组题”和“DeepSeek 智能组题”两个入口。
- 智能组题输入只包括当前岗位的 JD、简历、本地候选题和当前岗位面经，最多 12 题；服务端校验题型、target、来源 ID 和引用。
- 智能组题引文只容忍 Unicode NFKC、空白与有限标点差异；来源 ID、岗位归属和整题证据一致性不放宽。单题校验失败时跳过并返回警告，只有全部题目不可用时整批失败。
- 作答审阅不打分、不预测 Offer、不排名；结构包含已呈现证据、未说明事实、表达问题、判断／策略风险、JD 关联与经历佐证、单一优先改进项和下一条追问。
- 开源本地版支持环境 Key 与页面临时 BYOK。页面 Key 只存本机服务进程内存，不进入浏览器存储、导出、日志或 Git；重启服务后失效。

### 4.3 数据与隐私

- 当前没有账号系统或云数据库。工作台数据保存在当前浏览器 `localStorage`，键为 `xiangqian.interview-workbench.v6`。
- 页面右上角“导出”生成的 JSON 才是用户数据迁移文件；仅复制代码不会带走 JD、简历、面经、题库和训练历史。
- 原始 DOCX / PDF 不上传；解析后的正文会保存在浏览器中，只有用户主动点击 DeepSeek 时才发送必要文字。
- Agent Reach / OpenCLI 只复用用户当前已有的 Chrome 小红书会话；项目不读取或保存 Cookie，不登录、不绕过验证码或风控。
- 不提交 `.env.local`、真实简历、联系方式、API Key、Cookie、`node_modules/`、`dist/` 或临时采集输出。

### 4.4 Debug 协作方式

- 采用单代理、证据优先、测试闭环的轻量系统化 Debug：先稳定复现，再补可证明修复有效的失败测试，定位根因后实施最小改动，并运行与风险相称的验证。
- 可吸收 Superpowers `systematic-debugging` 的方法，但不启用多代理开发、自动复审循环或强制审查编排。
- 验收范围在开始修复前冻结；审查中发现的额外优化或新威胁模型作为后续事项记录，不自动扩大当前任务。

## 5. 技术结构

技术栈：Vite 7、React 19、TypeScript 5.9、Vitest；Node.js 22+。

| 文件 | 职责 |
|---|---|
| `src/App.tsx` | 页面编排、面经库切换、题目选择与浏览器语音交互 |
| `src/use-workbench-controller.ts` | 持久状态、恢复／保存、副作用和当前岗位派生数据 |
| `src/use-material-import-actions.ts` | JD、面经文本／Agent JSON 和简历文件导入 |
| `src/use-experience-collection.ts` | 深度采集状态、已有来源增量边界、覆盖结果和来源归档 |
| `src/use-source-question-reindex.ts` | 摘要优先的真题重新识别、必要时受限重读原文和结果归档 |
| `src/use-question-composition.ts` | 手动来源拼装、本地规则组题与 DeepSeek 智能组题 |
| `src/use-deepseek-session.ts` | DeepSeek 配置状态和本机会话 Key 生命周期 |
| `src/use-answer-review.ts` | 证据审阅请求、来源上下文、超时和错误状态 |
| `src/use-training-actions.ts` | 首轮建档、唯一追问、第二轮封闭与历史写回 |
| `src/use-workbench-backup-actions.ts` | 备份预览、确认恢复、导出和清除流程 |
| `src/workbench-initial-state.ts` | 脱敏初始状态、样例来源与初始题目 |
| `src/api-client.ts` | 同源 JSON 请求与统一错误解析 |
| `src/agent-reach-client.ts` | Agent Reach / OpenCLI 采集客户端 |
| `src/deepseek-client.ts` | DeepSeek 状态、会话 Key、组题和审阅客户端 |
| `src/components/*.tsx` | 材料、题库、练习和训练历史展示组件 |
| `src/types.ts` | 工作台、题目、来源、训练记录和 API 类型 |
| `src/engine.ts` | 本地题库生成、面经原题导入、简历主张筛选 |
| `src/target-scope.ts` | 公司 + 岗位 target key、跨岗位隔离、面经库索引 |
| `src/workbench-storage.ts` | localStorage 状态恢复和 V1–V6 兼容迁移 |
| `src/workbench-backup.ts` | JSON 备份字段白名单、解析、迁移与恢复预览 |
| `src/workbench-repository.ts` | localStorage 统一读取、保存和清除边界 |
| `src/resume-parser.ts` | TXT / Markdown / DOCX / 文字型 PDF 浏览器解析 |
| `src/search-strategy.ts` | 小红书精确／模糊起始词、深度查询矩阵与饱和常量 |
| `src/interview-question-extractor.ts` | emoji／圈号／中文编号及无问号真题的保守提取规则 |
| `src/interview-review-protocol.ts` | `interview.evidence-review.v1` 类型与严格解析 |
| `src/training-session.ts` | 有界两轮训练状态机 |
| `agent-reach-bridge.ts` | Vite serve-only OpenCLI 小红书 search → note 受限桥接 |
| `deepseek-interview-bridge.ts` | 会话 BYOK、智能组题、回答审阅与安全错误 |
| `vite.config.ts` | 4173 端口、开发期两个本机 API middleware、环境变量读取 |
| `src/styles.css` | 完整页面样式和响应式布局 |

开发服务才包含 OpenCLI 和 DeepSeek 桥接；静态 `dist/` 没有这些本机服务能力。

## 6. 版本记录索引

正式归档位于 `interview-workbench/docs/archives/`：

| 版本 | 模块 |
|---|---|
| V0.1 | 独立面试工作台 MVP |
| V0.2 | Agent Reach 小红书本地有限采集 |
| V0.3 | DeepSeek 作答分析 |
| V0.4 | 精确／模糊检索与 DOCX / PDF 简历解析 |
| V0.5 | 跨岗位面经来源隔离 |
| V0.6 | 我的面经库本地历史 |
| V0.7 | DeepSeek 会话级 BYOK |
| V0.8 | 视觉层级与简历编辑体验 |
| V0.9 | 无分数回答反馈协议 |
| V1.0 | JD 按岗位绑定；DeepSeek 证据审阅 |
| V1.1 | 来源化题库；练习区左右栏比例 |
| V1.2 | 两类题型收敛；有界两轮训练；经历佐证检查 |
| V1.3 | 简历题高价值主张和单一追问策略 |
| V1.4 | 本地候选 + DeepSeek 来源约束智能组题；真题筛选 |
| V1.6 | 完整备份恢复；localStorage 存储边界 |
| V1.7 | 小红书连续编号真题提取；四阶段采集统计 |
| V1.8 | DeepSeek 组题引文归一化；单题失败容错与警告 |
| V1.9 | React 页面测试环境；备份恢复与岗位切换隔离回归 |
| V1.10 | 采集与两轮训练页面回归；controller、clients 和展示组件渐进拆分 |
| V1.11 | 六组页面 use cases 下沉；每组通过四条页面回归 |
| V1.12 | 本地组题与 DeepSeek 单题容错页面回归；三类脱敏组题 Golden Cases |
| V1.13 | 三类 Golden Cases 真实 DeepSeek 观察 runner、统一 Rubric、脱敏报告与首轮观察 |
| V1.14 | 组题质量门禁收紧；法务情境与配比；第二轮真实观察及 V1→V2 对比 |
| V1.15 | DeepSeek 组题 warning 分类文案与页面级部分成功回归 |
| V1.16 | 桌面端 warning 结果卡、通知去重与 1280／1440px 视觉验证 |
| V1.17 | 小红书多查询深度采集、增量去重、饱和停止与覆盖报告 |
| V1.18 | 来源解析器版本、旧零题回填、富信息去重、手动重新识别与跨标签页提示 |
| v0.1.0 | GitHub 本机开源首发：安装说明、MIT 许可、迁移清单校准与干净环境验收 |

`2026-08-12-source-traceable-question-banks-v1.1.md` 记录过旧“四类题库”方案，已被 V1.2 的“两类主类别 + 来源标签”取代，不应恢复旧分类。

## 7. 当前已知问题与最高优先级

### 已解决（V1.7）：小红书正文真题提取与状态混淆

- 真题提取已从采集桥接拆到纯模块，支持 `1⃣️`、`1️⃣`、`①`、`一、`、`🌟1.` 等连续编号，以及“自我介绍”“谈实习经历”“反问”等无问号短题。
- 规则会截取原文中的题目主体，过滤流程、准备建议和作者解释；不补写问号或改写题意。
- API 和页面分别展示搜索覆盖、唯一来源、正文读取、含真题来源、真题提取和失败。正文成功但零真题的来源仍进入资料库，并标记“已保存正文摘要，尚未识别到真题”，不再暗示完整正文已长期保存或正文中一定没有题目。
- 2026-08-14 受控 OpenCLI E2E 验证了 20 条搜索命中、最多 3 篇串行正文读取和新统计契约；真实星标编号文章成功进入真题提取，随机无题文章保持零真题且不误报。
- 临时 `xsec_token` 仍只用于采集当次读取，不持久化；保存后的裸来源 URL 可能受小红书网页访问限制。

### 已解决（V1.8）：DeepSeek 智能组题引文误杀与整批失败

- 引文核验现先执行 Unicode NFKC，再移除空白与显式白名单内的中英文标点；内容字符仍必须连续存在于对应来源。
- 无效候选题 ID、重复候选题、无效来源、伪装面经原题、无法核验引文和缺少简历来源均按题跳过，并返回不含材料正文的 `warnings`。
- 一道衍生题包含多个来源时仍采用整题校验，任何一个来源失败都不会部分保留该题；只有最终零道可用题时才返回整批错误。
- 页面显示跳过数量并保留本批有效题目；作答审阅的 `interview.evidence-review.v1` 严格来源协议没有因此放宽。

### 已完成（V1.10）：四条页面核心流程与第一轮架构拆分

- 页面测试采用 Vitest 的 Node 默认环境，仅让 `.tsx` 页面用例进入 jsdom，避免改变既有桥接和纯函数测试运行语义。
- 备份恢复用例验证“文件读取与预览不会修改当前工作台 → 用户确认后完整替换 → 新状态写入 localStorage”。
- 岗位切换用例验证从历史面经库切换后只显示当前岗位 JD 和面经，并隐藏上一岗位题库；切回后原资料仍可恢复。
- 采集用例验证请求期间状态，以及搜索命中、正文读取、真题提取和失败四阶段结果；正文可读但提取失败的来源仍被保存并显示原因。
- 两轮训练用例验证“首轮审阅 → 唯一追问 → 次轮审阅 → 自动封闭并保存”，同时校验第二次请求携带首轮上下文和最终记录包含两轮回答。
- `tsconfig.test.json` 将页面测试纳入 `npm run check`，避免测试代码脱离 TypeScript 校验。
- 页面测试发现并修复了训练轮次标签与状态机 ID 格式不一致的问题；页面现按真实轮次顺序显示“主问题”和“唯一追问”。
- 持久状态和当前岗位派生数据已进入 `useWorkbenchController`；初始样例、API clients，以及材料、题库、练习和训练历史展示组件已从 `App.tsx` 分离。

### 已完成（V1.11）：页面 use cases 下沉

- 导入、采集、组题、DeepSeek 会话与审阅、训练转换和备份已经分别进入 7 个聚焦 hooks，没有合并成新的集中式 controller。
- 每迁移一组都运行 4 条页面核心流程；回归先后捕获了采集共享来源转换引用遗漏和进入追问时残留旧 setter 两个问题，修复并重跑通过后才继续。
- `App.tsx` 从 730 行收敛至 270 行，不再直接调用 Agent Reach / DeepSeek clients、题库引擎、训练状态机或备份解析器。
- `useWorkbenchController` 仍只负责持久状态、派生数据和 repository 副作用，没有吸收具体业务流程。

### 已完成（V1.12）：组题页面回归与三类 Golden Cases

- 页面回归新增“当前岗位材料生成本地来源化题库”和“DeepSeek 单题失败后保留有效题并展示安全跳过警告”。第二条同时验证请求只包含当前岗位来源，其他岗位来源不会发送。
- 产品、运营、法务三套脱敏 fixtures 位于 `src/test/fixtures/question-composition-golden.ts`，共同检查两类题库、5–12 题边界、标题去重、单一问题、逐字简历／JD 引文、岗位相关高价值经历、至少两个简历追问切口、面经原题保真与跨岗位隔离。
- Golden 自动门禁直接调用确定性本地引擎，确保每次运行可复现；DeepSeek 页面测试使用经过服务端校验后的模拟响应验证部分成功契约，不调用真实模型、不要求 API Key，也不通过重复调用挑选结果。
- 测试开发中先发现脱敏简历夹具实际不足 80 字，再发现“手动整理”会按产品规则重建手动来源 ID；均通过修正测试材料语义解决，没有放宽生产校验。

### 已完成（V1.13）：开发期 DeepSeek 观察与首轮报告

- `npm run observe:composition -- --confirm-live --output <path>` 会复用真实智能组题桥接，对产品、运营、法务三类脱敏 Golden Case 串行各调用一次；默认禁止 CI，单案失败不重试，也不重复调用挑选更好结果。
- 统一 Rubric 同时检查题量、来源与 provenance、面经原题保真、岗位隔离、去重、单一问题、类别覆盖和部分成功；机器检查与人工的岗位相关性、深挖价值、覆盖平衡分开记录。
- 报告只保留虚构岗位、输入指纹、题目标题、来源类型／ID、计数、warning 和检查结论，不保存 API Key、完整 JD／简历／面经或原始模型响应。
- 2026-08-14 首轮使用 `deepseek-v4-pro` 完成 3 次调用，三案各保留 12 道题、0 条服务端 warning，来源核验、原题逐字保留和岗位隔离全部通过。
- 三案均需关注少数双重追问和模板式 JD 复述；法务案另有简历深挖 8 道、岗位能力 4 道的配比偏斜，专业题应增加情境化判断。该结论直接记录，没有重跑择优。
- 首轮脱敏报告位于 `docs/observations/2026-08-14-deepseek-question-composition-golden-observation-v1.md`，结构化副本为同名 JSON。

### 已完成（V1.14）：组题质量收紧与第二轮观察

- DeepSeek 提示和服务端门禁现在共同拒绝“JD 提到……如何在实际工作中做到”式模板题；模型衍生题必须只有一个结尾问号和一个主要问题，多问句按单题跳过。
- 最终题库同时包含两类题时，简历深挖与岗位能力题数量差最多为 2；配比裁剪优先保留逐字面经原题。
- 法务、法律、合规岗位明确要求至少 2 道模型生成的业务情境题，题面需要具体冲突或约束；观察 Rubric 新增模板 JD、类别配比和法务情境覆盖。
- 第二轮仍对产品、运营、法务各调用一次 `deepseek-v4-pro`，不重试。三类模板 JD 均由 3 道降至 0，多焦点题均降至 0；法务配比由 8:4 改为 3:3，并生成 2 道模型情境题。
- 产品和运营第二轮各出现 1 道模型复制已精选面经原题，观察报告按失败保留该事实。随后新增 `DUPLICATE_QUESTION_TITLE` 服务端跳过规则和回归测试，但没有重跑第二轮或修改题目清单。
- 法务情境统计初次漏掉“业务团队……”和“你发现……”开头，分类器修正后按实际题面记为 2；该修正不涉及重新调用模型。
- 独立 V2 报告与结构化 JSON 位于 `docs/observations/2026-08-14-deepseek-question-composition-golden-observation-v2.*`，跨轮比较位于 `docs/observations/2026-08-14-deepseek-question-composition-golden-v1-v2-comparison.md`。

### 已完成（V1.15）：页面 warning 分类回归

- `question-composition-warning.ts` 将桥接 warning code 聚合为稳定的用户分类，不直接展示服务端原始 message。
- 页面明确区分模板式 JD 题、重复题、包含多个问题的题、来源或引文无法核验的题；`CATEGORY_BALANCE_LIMIT` 单独显示为配比调整，未来未知 code 归入“其他未通过质量检查”。
- 页面级部分成功回归一次模拟四类 warning，验证四类数量均可见、2 道有效题继续展示和持久化、其他岗位来源仍不发送、原始服务端 message 不进入页面。
- 该模块只改变 warning 的解释方式，不改变服务端校验、题库状态、网络边界或 V1/V2 真实观察报告。

### 已完成（V1.16）：桌面端 warning 体验收敛

- 部分成功不再使用 9px 单段 warning；材料区改为结构化结果卡，首要信息是“已保留 N 道有效题”，分类原因使用可换行标签，底部明确“有效题库已经保存，可以继续练习”。
- 部分成功时不再同时设置全局成功 notice，也不自动滚动到题库；用户先看到本次质量结果，可通过“查看有效题目”主动进入题库。完全成功时仍保留原有全局通知和自动滚动。
- 生成栏改为两列网格，说明区和按钮区保持第一行，结果卡固定占第二整行；warning 增多不会与按钮争抢同一文本列。
- 真实浏览器在 1280×900 和 1440×900 下完成检查：均无横向溢出，按钮区宽度稳定为 264px；四类共 8 条 warning 的压力夹具中，结果卡高度均为 129px，标签正常换行，未侵入材料卡或按钮区域。
- 真实工作台在 1280px 下材料区／生成栏宽度为 1137px，在 1440px 下为 1236px；两档均保持两列材料网格和稳定按钮区。临时视觉夹具在检查后已删除，不进入项目交付。

### 已完成（V1.17）：小红书深度采集

- 单次用户触发会从可编辑起始词扩展到最多 6 组公司内查询，覆盖精确岗位、岗位族、面试、面试题和面试轮次；不会跨公司，也不会转为后台任务。
- 至少执行 4 轮后，连续 2 轮没有新增唯一来源即判定饱和；达到 20 篇新来源、约 6 分钟总时限或查询计划结束也会停止。
- 采集按小红书笔记 ID 跨查询去重，并接收当前岗位已有 URL；再次采集会跳过已有来源，只读取增量。临时 token URL 仍只在本机进程内用于当次正文读取。
- 页面覆盖报告展示查询计划／执行数、累计命中、唯一来源、已有跳过、新增读取、含真题来源、真题数、失败和停止原因，可展开逐轮查询与新增数量。
- 页面采集回归升级为深度采集契约；桥接测试覆盖多查询、跨轮重复、已有来源跳过、饱和停止、部分读取失败和零题摘要来源。
- 2026-08-18 首个真实“字节跳动 · 法务”观察执行 4 组查询，发现 19 个唯一来源，成功读取 16 篇，其中 11 篇共提取 51 道真题，3 篇正文失败，耗时约 6 分 18 秒；完整记录见 `docs/observations/2026-08-18-xiaohongshu-deep-collection-byte-legal-v1.md`。
- 本轮全部来源来自最后一组“一面 二面”查询，前三组为搜索失败、零命中和超时；因此“查询计划执行完”不能视为达到饱和。下一开发项应分开搜索失败与正文失败、修正停止文案，并让高产查询之后仍有最多两轮受限饱和检查。

### 已完成（V1.18）：来源真题重新索引与多标签一致性

- `ExperienceSource.questionParserVersion` 记录来源所用题目解析器版本；当前纯规则解析器为 V2。恢复旧备份或本地缓存时，空题来源会先对已保存摘要执行一次安全回填，并报告重新检查来源数和恢复题数。
- 连续编号规则补充“事件、工作、职责”等保守标签，真实观察中的“如何看待和应对法务实习中的基础性工作”和“谈仅退款规则的理解”已进入回归。
- 重复来源改为按“岗位 + URL”合并；优先保留题目更多、摘要更完整的记录，并固定沿用旧来源 ID，避免已有题库和训练引用失效。没有 URL 时仍按岗位和标题去重。
- 页面对零题来源使用“已保存正文摘要，尚未识别到真题”，并显示当前待识别来源数和“重新识别真题”按钮。点击后先解析摘要；仍为零的小红书 Agent Reach 来源才通过本机桥接重读正文，单次最多 10 篇、总时限约 3 分钟，完整正文不持久化。
- 工作台监听当前存储键的跨标签页变化，但不自动覆盖本页状态；用户可选择“加载最新数据”或“稍后”。
- 新增来源重读和跨标签确认刷新两条页面回归；目前 16 个测试文件、123 项测试全部通过，正式构建通过。桌面实页确认字节跳动法务当前有 5 篇待识别来源，新按钮和状态文案稳定显示；没有替用户触发原文重读。

### 已完成（v0.1.0）：GitHub 本机开源发布收尾

- README 首屏明确 Node.js 22+、安装启动命令、可选 DeepSeek 与 Agent Reach / OpenCLI、无外部能力时仍可用的本地闭环、浏览器数据位置和卸载前导出步骤。
- 子项目补充与根仓库一致的 MIT LICENSE，并声明项目与第三方服务无隶属或背书关系；采集仅用于主动、有界、只读的本机操作，不支持无人值守或批量抓取。
- `MIGRATION_MANIFEST.txt` 已把完整备份恢复更新为现状，并将 LICENSE 纳入迁移保留清单。
- 冷启动首次暴露 `@types/node` 依赖曾由父目录间接满足；现已在 `devDependencies` 显式声明 Node 22 类型，确保独立复制后可以构建。
- 新增“页面配置会话 Key 不回显、不写入 localStorage”和“采集失败保留材料并显示可操作错误”两条页面回归。当前为 16 个测试文件、125 项测试，页面核心流程 10 条。
- 在排除 `node_modules/`、`dist/`、`.env.local`、缓存和日志的全新临时目录中，按 README 执行 `npm install`，随后完整测试和正式构建通过；开发服务可启动，首页和 DeepSeek 配置状态接口可访问。生产构建仍只有既有 PDF.js 大 chunk 警告。

### P2：修正真实采集覆盖语义

下一步先根据“字节跳动 · 法务”真实观察修正采集报告：顶层分开搜索失败和正文失败；`query_plan_exhausted` 改为“已尝试全部搜索词”；高产查询之后仍执行最多两轮受限补充查询以判断是否趋于饱和；长请求展示当前搜索轮次和正文读取进度。完成回归后再选择第二个真实岗位观察，不引入后台队列、数据库或新服务。第三轮 DeepSeek 组题观察继续保留为后续独立任务，不覆盖既有 V1/V2 报告。

### 其他边界

- 浏览器语音识别不是后端语音转写；兼容性和隐私取决于浏览器实现。
- 扫描 PDF、图片型 PDF、加密 PDF 和旧 `.doc` 不支持。
- localStorage 没有跨设备同步；面经库不能改名、单独删除或云同步。
- 尚未用大量真实简历、JD 和多岗位数据建立生成质量评测集。
- 生产构建有 PDF.js 大 chunk 警告，不影响当前构建。

## 8. 运行、验证与迁移

### 本地运行

```bash
cd interview-workbench
npm install
npm run dev -- --host 127.0.0.1
```

地址：`http://127.0.0.1:4173/`。

### 自动验证

```bash
npm run check
npm test
npm run test:ui
npm run build
```

截至 v0.1.0 发布检查点，16 个测试文件、125 项测试全部通过；页面核心流程为 10 条，并覆盖备份恢复、岗位隔离、采集状态与失败、两类组题、会话 Key、两轮训练、来源重读和跨标签页刷新。独立干净目录的安装、类型检查、完整测试、正式构建和开发服务启动均通过；生产构建仍只有既有 PDF.js 大 chunk 警告。

### 用户数据迁移

1. 在旧工作台点击右上角“导出”，保存 JSON。
2. 复制整个 `interview-workbench/`，不要复制 `node_modules/`、`dist/`、`.env.local`。
3. 新环境安装依赖并启动工作台。
4. 在新工作台点击右上角“导入备份”，核对公司岗位、来源、题库、简历字数和训练记录预览，再确认完整替换。无效文件和写入失败不会改变当前工作台；当前版本不做自动合并。
5. 在新环境重新配置 DeepSeek Key；Key 不在导出文件中。
6. 小红书 OpenCLI 能力需要新机器上的 Chrome、OpenCLI 扩展和用户自己的登录会话。

### 代码迁移前清理

- 排除：`node_modules/`、`dist/`、`.env.local`、日志、临时文件、浏览器配置和 Cookie。
- 保留：源代码、`package.json`、`package-lock.json`、TypeScript 配置、测试、README、`HANDOFF.md`、`docs/`、`.env.example`。
- v0.1.0 发布提交只纳入 `interview-workbench/`；根仓库其他既有改动不属于本次发布，不应混入该提交。

## 9. 事实来源优先级

发生冲突时按以下顺序判断：

1. 当前代码和测试；
2. `docs/DECISIONS.md` 中“独立面试工作台”确认决策；
3. `interview-workbench/README.md` 与本文件；
4. 较新版本归档；
5. 较旧归档和聊天记录。

本文件最初创建于 2026-08-13，并已更新至 V1.18；不替代后续代码与更新归档。
